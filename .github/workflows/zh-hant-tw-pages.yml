name: Publish zh-Hant-TW tool directory

on:
  push:
    branches:
      - codex/zh-hant-tw-localization
    paths:
      - README.zh-Hant-TW.md
      - _config.yml
      - _layouts/**
      - index.md
      - style.css
      - localization/zh-Hant-TW/**
      - .github/workflows/zh-hant-tw-pages.yml
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: zh-hant-tw-tool-directory-pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out source
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Configure GitHub Pages
        uses: actions/configure-pages@983d7736d9b0ae728b81ab479565c72886d7745b # v5

      - name: Validate localization source and coverage
        run: ./localization/zh-Hant-TW/scripts/check.sh

      - name: Build the Jekyll site
        uses: actions/jekyll-build-pages@44a6e6beabd48582f863aeeb6cb2151cc1716697 # v1
        with:
          source: ./
          destination: ./_site

      - name: Validate the rendered tool directory
        run: node ./localization/zh-Hant-TW/scripts/check-rendered.mjs ./_site

      - name: Upload GitHub Pages artifact
        uses: actions/upload-pages-artifact@56afc609e74202658d3ffba0e8f6dda462b719fa # v3
        with:
          path: ./_site

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@d6db90164ac5ed86f2b6aed7e0febac5b3c0c03e # v4
